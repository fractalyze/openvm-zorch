# zorch runtime dependencies.
#
# Pulled from the Fractalyze package index alongside upstream PyPI:
#   pip install -r requirements.in \
#     --extra-index-url https://fractalyze.github.io/pypi/simple/
#
# zorch is proving-scheme- and zkVM-agnostic, so it deliberately does NOT
# depend on any zkVM package (riscv-witness, rw-constraints, …). zk_dtypes
# (native finite-field dtypes) arrives via jax but is pinned here directly:
# each jax wheel imports a specific zk_dtypes ABI — the extension-field
# lowering reads `efinfo.modulus_low_coeffs` (from 0.0.6), and `jax/_src/
# dtypes.py` reads `zk_dtypes.pallas_sf` (from 0.0.7, the dev20260619 wheel).
# jax's metadata does not floor the version, so bump this in lockstep with the
# jax pins above or imports fail at `import jax`.
absl-py
numpy
jax==0.0.5.dev20260622080804
jaxlib==0.0.5.dev20260622080804
jax-cuda12-plugin==0.0.5.dev20260622080804
zkx-cuda-pjrt==0.0.5.dev20260622080804
zk-dtypes==0.0.7

# Benchmark harness (shared across fractalyze repos); used only by the
# //zorch/...:*bench* py_binary targets, not by the zorch wheel itself.
zkbench==0.4.0
